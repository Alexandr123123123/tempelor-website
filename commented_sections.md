# Закомментированные разделы (Архив)

В этом файле мы ведем учет разделов, которые временно или навсегда убраны с сайта (закомментированы), но код которых всё ещё остался в проекте на случай, если они понадобятся.

---

## 23.05.2026
* **Скрыт раздел:** `About` (оригинальный блок "About Tempelor", который шел до нашего коллажа).
* **Причина:** Заменен на наш новый креативный блок "About Tempelor 1" (виджеты `About1` и `AboutFinal`).
* **Файл, где закомментировано:** `src/pages/Home/ui/HomePage.jsx` (компонент `<About />` обернут в `//` или `{/* */}`).

Сейчас на сайте отображается только раздел `About Tempelor 1` (интерактивный редактор и статичная финальная версия).
